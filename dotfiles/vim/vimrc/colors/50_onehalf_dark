" load onehalf dark theme
if !exists('+packpath')
  execute pathogen#infect('pack/{}/opt/onehalf')
else
  packadd onehalf
endif
colorscheme onehalfdark

" Change the theme of light line
let g:lightline.colorscheme='onehalfdark'

